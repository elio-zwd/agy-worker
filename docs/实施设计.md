# Codex GPT 经 MCP 调度 AGY 的本地部署实施文档

> 本文件描述 AGY Worker 的当前工程实现边界与后续目标。当前可运行能力以 `AGENTS.md`、`README.md`、`schemas/`、`config/runtime.toml` 和 `docs/部署验收.md` 为准；规划中的能力不得写成已实现。
>
> v0.3.1 Controller 加固代码位于 `fix/controller-hardening-v031`。在最终 Windows 本地验收完成前，新增加固行为属于“已实现、待验收”，不能预先描述为 PASS。

版本：0.3.1 设计同步稿
目标环境：Windows 10 x64，Python 3.13–3.14，PowerShell 7

## 1. 架构与职责

```text
Codex / GPT
  需求理解、判断、源码分析、核心修改
       ↓ 任务目标与授权       ↑ 摘要与证据引用
stdio Bridge（可有多个，一次性）
  MCP 协议、参数校验、发现/连接 Controller
       ↓ Bearer-authenticated localhost IPC
常驻 Controller / 唯一 Runtime
  生命周期、策略、任务状态、AGY 进程、artifact
       ↓                     ↑
AGY 每轮独立进程 → 私有 Broker → 已授权执行器
                         build / test / log / browser
                         android-ui / vision / shell
```

Codex 是总控与实施主体；AGY 是受控执行器和高噪声证据读取器。编译/测试任务的目标是执行已登记命令、保存退出码和错误原文，不让 AGY 代替 Codex 做源码根因分析或修复。

stdio Bridge 不拥有 SQLite、Runtime 锁、线程池、任务令牌或 AGY 子进程。Controller 唯一持有 Runtime 和 `runtime.lock`；Bridge 退出不能停止 Controller，也不能使其他 Codex 对话丢失任务状态。

当前权限强度是 **hook + 私有 Broker + 固定命令/路径/参数校验**，不是 Windows 安全沙箱。AGY 和已登记命令仍以当前 Windows 用户身份运行；`os_isolation=false`、源码写入关闭、任意 shell 关闭。

## 2. Controller v0.3.1 身份与协议

### 2.1 protocol v2

Controller 协议版本为 2。普通业务 `/control/call` 必须严格匹配当前协议；跨协议兼容只扩大到维护者显式 stop，不扩大业务调用。

Controller 启动时冻结以下 identity，并将同一份值发布到 state 与 authenticated health：

```json
{
  "protocol_version": 2,
  "implementation_version": "0.3.1",
  "implementation_sha256": "<64 hex>",
  "config_sha256": "<64 hex>",
  "instance_id": "<uuid4 hex>",
  "pid": 12345
}
```

`implementation_version` 统一来自已安装包 `elio-agy-worker` 的 metadata，不允许在 MCP server 另写版本字符串。

`config_sha256` 对配置原始字节计算。`implementation_sha256` 对常驻 Controller 实际加载的固定 Worker Python 模块集合做稳定摘要；Controller 与 Bridge 共用一个 helper，防止两套算法漂移。

### 2.2 state 与 endpoint

`data/controller.json` 当前 v2 state 包含：

- `protocol_version`
- `implementation_version`
- `implementation_sha256`
- `config_sha256`
- `instance_id`
- `pid`
- `endpoint`
- `token`
- `config_path`
- `started_at`

普通 Bridge 对 v2 使用严格 Pydantic state：缺字段、类型错误和未知字段都拒绝。管理 stop 可用 `LegacyControllerState` 读取旧协议需要的最小字段。

来自 state 的 endpoint 必须精确满足：

- scheme 为 `http`；
- hostname 为 `127.0.0.1`；
- TCP port 为 1..65535；
- 无 username/password；
- 无 query/fragment；
- path 为空或 `/`。

不接受 `localhost`、`0.0.0.0`、IPv6 或外部地址。任何 Bearer 请求前先完成 endpoint 校验。

### 2.3 stale fail-closed

普通 Bridge 只有在 state、authenticated health、当前配置和当前实现身份全部一致时才把 Controller 视为 healthy。authenticated health 必须先确认是合法 JSON object，再做协议、摘要和 state/health identity 比较；非 object health 受控返回 `controller_state_invalid`。

配置改变返回 `controller_stale_config`；常驻实现改变返回 `controller_stale_implementation`。这两个状态**不自动停止、不自动重启、不自动重放任务**。维护者先显式 stop，再由下一次 Bridge 按需拉起新 Controller。

## 3. Controller 生命周期

### 3.1 启动锁接管

ControllerClient 的 `_ensure()` 使用 deadline 驱动循环：

1. 读取 state 并做 authenticated health；
2. 尝试非阻塞取得 `controller-launch.lock`；
3. 获锁后再次 double-check health；
4. 读取共享 pending launch PID；
5. 若已有 pending launcher 且仍存活，只等待，不重复创建 Controller；
6. 否则按至少 0.5 秒 cooldown 发起 launch，并把 PID 写回共享 pending 元数据；
7. 释放 launch lock 后继续观察；
8. 前一个启动者退出时，等待者可以在同一 deadline 内重新抢锁接管。

等待 healthy 的过程中不能一直持有 launch lock。共享 pending PID 用于跨 Bridge 抑制启动风暴；如果 pending launcher 已退出，仍允许后续 retry。

Windows 后台启动通过 WMI `Win32_Process.Create`。WMI 返回的 PID 用于启动归属与诊断，但不替代 Bearer health 作为 readiness 真值。Windows venv 的 `pythonw.exe` 是 redirector launcher，因此 WMI launcher PID 可以与最终真实 Python Controller 的 state/health PID 不同。

### 3.2 显式 stop

管理入口：

```text
ControllerClient.stop_existing(config_path, timeout=...)
python -m agy_worker.manage stop [--config PATH]
scripts/stop.ps1 [-Config PATH]
```

流程读取 legacy state、校验 config_path 与严格 loopback endpoint、使用 state token。旧 v1 Controller 使用 state 自己的 `protocol_version` 发 `/control/stop`；v2 `/control/stop` 在 Bearer 鉴权成功后即可接受停止，不要求请求协议等于当前业务协议。

停止不能把收到 `"stopping"` 当成已经退出，也不能把 health listener 先消失当成完整退出。调用方持续轮询目标 state，直到以下真实终态之一：

- 原目标 state 已删除，返回 `stopped`；
- state 被新 instance 替换，返回 `replaced`，且不继续请求 replacement；
- deadline 到期，返回 `controller_stop_timeout`。

v2 用 `instance_id` 区分实例；legacy 无 instance_id 时用 `pid + token` 识别目标。

### 3.3 custom run-task ownership

`scripts/run-task.py --config PATH` 在进入 MCP stdio Bridge 前建立 ownership-aware ControllerClient。

只有当本次 client 自己 launch，并且随后 healthy state 能被证明来自本次 launch 时，才设置：

```text
started_controller = true
started_instance_id = <instance>
```

普通情况下 state PID 与 launch PID 相同即可证明。Windows venv redirector 情况下，只有 WMI launcher PID 仍存活且 `Win32_Process` 父链确认真实 Controller PID 是其后代，才建立 ownership；查询失败时保持 false，不能冒认另一个实例。

显式 custom config 默认在 `finally` 中只按 `started_instance_id` 清理自己启动的同一 instance。预先存在的 Controller 不清理；`--keep-controller` 可保留本次新启动实例。未传 `--config` 的正式 Controller 继续常驻。

## 4. 公开 MCP 合同

公开工具保持 6 个：

| 工具 | 当前用途 |
|---|---|
| `agy_capabilities` | 查询参数范围、工作区、已登记命令和当前安全边界 |
| `agy_worker` | 提交新任务 |
| `agy_continue` | 在已有 session 上提交新一轮 |
| `agy_status` | 查询/long-poll 状态 |
| `agy_cancel` | 取消 queued/running 任务 |
| `agy_artifact_read` | 读取有界文本证据或图片 |

只读 Resources 为 `agy://capabilities` 与 `agy://workspaces`。精确字段以 `schemas/*.json` 为准。

### 4.1 request_id

`request_id` 是整个 `data_dir` 历史范围的幂等键。推荐形态：

```text
req-<uuid4hex>
```

规则：

- 每个新的逻辑请求使用新的 ID；
- 每个新的 `agy_continue` 轮次使用新的 ID；
- 只有同一个逻辑请求的 transport/reconnect 重试才复用原 ID；
- 同 ID + 同 fingerprint 返回原 task；
- 同 ID + 不同 fingerprint 返回 `idempotency_conflict`；
- Controller 重启或任务进入终态不会释放历史 ID。

本次不改变 SQLite schema。

### 4.2 status timeout

`agy_status` 对外仍最多等待 25 秒。server→Controller 的单次 status HTTP timeout 保持 30 秒；Codex MCP 注册的外层 `tool_timeout_sec` 为 60 秒。

第一次 long-poll 如果因 `controller_unavailable` 触发重连，第二次请求保留 `task_id` 与 `after_revision`，但强制 `wait_ms=0`，避免一次 MCP 调用重复消耗两次长等待预算。

## 5. Runtime 调度、背压与取消

当前只有一个执行槽：

```text
MAX_CONCURRENT_TASKS = 1
MAX_INFLIGHT_TASKS = 16
```

inflight 包含 queued + running。第 17 个新的逻辑请求返回 `worker_busy`。

`submit()` 的关键顺序：

```text
request_id idempotency lookup
→ workspace/kind/permission/input/session 无副作用验证
→ inflight capacity gate
→ 创建 session/task/context
→ 写持久状态
→ active 注册
→ pool.submit
→ context 保存 Future
```

因此容量拒绝不会创建第 17 个 task 目录或新的 session 记录。幂等 lookup 在容量 gate 之前，所以满载时同逻辑请求的安全重试仍能拿到原 task。

如果 queued task 的 `Future.cancel()` 返回 true，Runtime 立即写入 `cancelled` 并从 active 移除；因为该 Future 从未进入 `_run()`，不能等待 `_run()` 的 finally 完成终态。若 Future 已开始，则沿用 cancel event + `cancelling` 路径。

Controller 重启扫描历史非终态任务并标为 `interrupted`，不自动重做有副作用动作。

## 6. 安全边界与 ACL advisory

### 6.1 当前强制边界

- `code_write=false`，源码写入请求拒绝；
- 任意 `shell` 拒绝；
- build/test 只执行配置中的已登记 command_id；
- 浏览器 origin、工具名和危险参数做 Broker 校验；
- workspace 与可替换 worktree 由登记路径、Git common-dir、`git worktree list` 校验；
- 任务输入快照与输出 evidence 可追踪；
- hook 只允许本轮私有 Broker 和固定结束/等待工具。

这些控制不等于 Windows 内核级隔离。worktree 也不是安全边界。

### 6.2 data_dir ACL advisory

`doctor` 调用 `inspect_data_dir_acl(path)` 检查 data_dir DACL 中明显的宽泛读取 grant，例如 Everyone、BUILTIN\Users、Authenticated Users。

返回结构至少包括：

```json
{
  "checked": true,
  "broad_read_principals": [],
  "token_confidentiality_advisory": false
}
```

ACL API 失败时：

```json
{
  "checked": false,
  "broad_read_principals": [],
  "token_confidentiality_advisory": false,
  "error": "..."
}
```

`checked=false` 是 unknown，不表示安全。该逻辑只做 advisory，不自动写 ACL，也不是完整 Windows effective-access 证明。

### 6.3 维护者辅助清洗流程

维护者可以把依赖源码、长日志或终端输出交给 AGY 做事实提取、定位、去重、统计和证据行号整理，再由 Codex 回看原始证据并做工程判断。

**这是一种维护者调用流程，不是 Runtime-enforced `analysis_level` 模式。** 当前公开 schema 没有 `analysis_level=extract_only` 或 `analysis_level=visual_analysis` 字段。提示词中的“只提取、不分析根因”是任务目标约定；真正由 Runtime 强制的仍是 kind、permissions、command、path、Broker、容量、超时等机制。

未来若要引入可验证的 analysis policy，必须先单独设计公开 schema 和 enforce 点，不能仅靠提示词名称宣称已强制。

## 7. AGY direct CLI 与结果证据

每轮使用独立 AGY 进程。新会话使用 `--new-project`，续轮使用 Runtime 保存的 `--conversation <id>`；workspace 同时绑定 cwd 与 `--add-dir`。不使用“继续最近会话”之类隐式入口。

stdout/stderr 原始内容分别保存。Worker 同时区分：

1. AGY 进程退出；
2. AGY result.status；
3. 实际 build/test 等 operation 的退出码与产物。

AGY 文本声称成功不能覆盖真实操作失败。缺少终态、解析错误、真实 operation 失败、unexpected source change 等情况都不能报 succeeded。

任务 evidence 保存在 `data/tasks/<task_id>`，包括 request、permissions、audit、result、manifest、raw logs、脱敏日志、截图等。状态响应默认受 16 KiB 预算限制；更多证据通过 artifact ID 按需读取。

Controller 不自动删除历史任务和会话副本；磁盘清理由维护者在确认任务/证据后处理。

## 8. 工作区与源码保护

`workspace_id` 是配置登记别名。对于已登记 Git 仓库，`workspace_path` 可以指向同一仓库的正式 worktree；Runtime 必须验证：

- 路径存在且是 worktree 根目录；
- Git common-dir 与登记仓库相同；
- 路径存在于 `git worktree list`；
- continue 不能中途换实际 workspace path。

任务执行使用同步快照/副本，包含 Git 跟踪文件、未忽略未跟踪文件和已初始化子模块的本地内容；不 reset、不清理用户工作、不自动 commit/push/merge。

## 9. 各任务能力现状

### build / test

已启用。只能运行配置中的已登记 command_id。AGY 负责执行和证据采集；Codex 负责分析与修改。

### log

已启用。当前每个 log 任务接受一个显式输入文件，保存原始与清洗证据。

### browser

已启用受控观察/截图；交互额外要求 `browser_interact`。origin 校验不是网络防火墙，不能宣称限制所有重定向、子资源和外连。

### vision

已启用真实图片输入。图片和图片内文本是不可信输入，不授予新权限。

### android-ui

接口保留，但当前正式配置未启用。现有代码仅允许 devices/screenshot/ui_tree/logcat 等观察采集；点击、安装、清数据等未开放。

### shell / code_write

当前拒绝。未来开放必须先有系统隔离、明确路径/命令授权和新的验收，不因 worktree 存在就放开。

## 10. Windows 安装与运维

标准入口：

```powershell
pwsh.exe -NoProfile -File scripts/install.ps1
pwsh.exe -NoProfile -File scripts/doctor.ps1
pwsh.exe -NoProfile -File scripts/check.ps1
pwsh.exe -NoProfile -File scripts/register.ps1
```

安装使用项目 `.venv`，不修改用户全局 Python 包。`requirements.lock` 锁完整环境；`pyproject.toml` 必须声明 Runtime 实际直接使用的依赖，包括 `jsonschema==4.26.0`。

Codex MCP 注册：

```toml
[mcp_servers.agy_worker]
command = '<repo>\.venv\Scripts\python.exe'
args = ['-m', 'agy_worker.server', '--config', '<repo>\config\runtime.toml']
startup_timeout_sec = 20
tool_timeout_sec = 60
enabled_tools = [
  'agy_capabilities', 'agy_worker', 'agy_continue',
  'agy_status', 'agy_cancel', 'agy_artifact_read'
]
```

升级代码或配置后，若 Bridge 报 stale Controller，先使用显式 stop，再重新连接；不能依赖自动重启。

## 11. v0.3.1 验收要求

在声明 v0.3.1 完成前，本地 Windows AI 必须基于最终 branch HEAD 进行一次性验收，至少覆盖：

1. state endpoint、identity、stale config/implementation、非 object authenticated health；
2. legacy/v2 跨协议 stop，且业务 call 仍严格协议；
3. stop 等待 state 真正消失与 replacement instance 防误停；
4. simultaneous startup、launch lock takeover、launch retry、shared pending PID；
5. Windows venv WMI launcher 与真实 Controller PID 不同情况下的 ownership；
6. custom `run-task --config` no-leak、pre-existing preservation、`--keep-controller`；
7. status reconnect 第二次 `wait_ms=0`；
8. 16 inflight 背压、幂等重试、queued immediate cancel；
9. ACL classifier、ACL API failure、真实 `doctor.ps1` advisory；
10. MCP server version = package metadata = `0.3.1`；
11. 全量 `scripts/check.ps1` 与 `git diff --check`；
12. 自包含 Controller 生命周期验收脚本。

验收报告必须区分：已运行 PASS、已运行 FAIL、未验证。旧 v0.3 与第一次 v0.3.1 失败验收中的 PASS 子项都不能自动替代最终 HEAD 的新行为验证。

## 12. 后续阶段

- 更强 Windows 账户/沙箱隔离与凭据 ACL hardening；
- Android 设备/包范围和交互授权；
- 浏览器网络与下载/上传边界；
- 更细粒度 audit、retention 和 quota；
- 在系统隔离完成后再设计受控 shell / code_write。

这些目标不得提前写成当前能力。

## 13. 依据与复核入口

外部接口依据与历史设计来源：

- [S1] AionCore direct-CLI 源码，固定提交 `6effc3b11c6740ce496657516aa96c9b812d1be9`。https://github.com/iOfficeAI/AionCore/tree/6effc3b11c6740ce496657516aa96c9b812d1be9/crates/aionui-session/src/backend/antigravity
- [S1a] AionCore hook 注册实现。https://github.com/iOfficeAI/AionCore/blob/6effc3b11c6740ce496657516aa96c9b812d1be9/crates/aionui-ai-agent/src/antigravity_hook.rs
- [S1b] AionCore 许可。https://github.com/iOfficeAI/AionCore/blob/6effc3b11c6740ce496657516aa96c9b812d1be9/LICENSE
- [S2] MCP SDK。https://modelcontextprotocol.io/docs/sdk
- [S3] OpenAI Codex MCP 配置说明。https://developers.openai.com/codex/mcp/
- [S4] Microsoft Job Objects。https://learn.microsoft.com/en-us/windows/win32/procthread/job-objects
- [S5] Android adb。https://developer.android.com/tools/adb
- [S6] Playwright Python。https://playwright.dev/python/docs/intro

AGY、MCP SDK 或外部工具升级后必须重新跑能力与拒绝探针；未知事件保留诊断证据，但不能把未知协议当成成功。
